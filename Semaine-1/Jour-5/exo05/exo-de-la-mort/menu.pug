ul
    each menuItem in menuItems
        if menuItem.isActive
            li.nav-active
                a(href=menuItem.path) #{menuItem.title}
        else
            li
                a(href=menuItem.path) #{menuItem.title}